[build]
  command   = "yarn build-storybook"
  publish   = "storybook-static"

[build.environment]
  NODE_VERSION = "20"

# Ensure clean installs on Netlify
[build.processing]
  skip_processing = false
