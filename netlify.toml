[build]
  command   = "node scripts/generate-ai-manifest.mjs && yarn build-storybook"
  publish   = "storybook-static"

[build.environment]
  NODE_VERSION = "24"

[build.processing]
  skip_processing = false

[functions]
  directory    = "netlify/functions"
  node_bundler = "esbuild"
