{/* src/docs/Introduction.mdx */}
import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Themes/Introduction" />

export const th  = { padding: '10px 16px', textAlign: 'left', fontSize: 12, fontWeight: 600, color: 'var(--color-foreground-muted)', textTransform: 'uppercase', letterSpacing: '0.06em', borderBottom: '2px solid var(--color-border)' }
export const td  = (mono) => ({ padding: '9px 16px', fontSize: 13, fontFamily: mono ? 'monospace' : undefined, borderBottom: '1px solid var(--color-border)', color: 'var(--color-foreground-secondary)' })
export const td0 = { padding: '9px 16px', fontSize: 13, borderBottom: '1px solid var(--color-border)', color: 'var(--color-foreground)', fontWeight: 600 }
export const tbl = { width: '100%', borderCollapse: 'collapse', marginBottom: 32, background: 'var(--color-surface)', borderRadius: 7, overflow: 'hidden', border: '1px solid var(--color-border)' }

# @geomak/ui

A production-grade React design system built on [Radix UI](https://www.radix-ui.com/) primitives and [Tailwind CSS](https://tailwindcss.com/). Every visual decision is driven by a fully parameterized CSS custom-property token layer — swap colors, scale, density, or motion system-wide with a single override.

---

## Quick start

```bash
npm install @geomak/ui
# or
yarn add @geomak/ui
```

**1. Import the stylesheet** (once, at your app entry):

```ts
import '@geomak/ui/styles'
```

**2. Add the font** to your HTML `<head>`:

```html
<link rel="preconnect" href="https://rsms.me/">
<link rel="stylesheet" href="https://rsms.me/inter/inter.css">
```

**3. Use components:**

```tsx
import { Button, ThemeProvider } from '@geomak/ui'

export default function App() {
  return (
    <ThemeProvider colorScheme="system">
      <Button variant="primary" content="Launch" />
    </ThemeProvider>
  )
}
```

---

## Architecture

The system is built in three layers:

<table style={tbl}>
  <thead>
    <tr>
      <th style={th}>Layer</th>
      <th style={th}>What it is</th>
      <th style={th}>When to use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={td0}>CSS custom properties</td>
      <td style={td(false)}>Runtime token values in <code>:root</code> and <code>.dark</code></td>
      <td style={td(false)}>Override at any scope — <code>:root</code>, <code>.my-section</code>, <code>{'[data-theme="compact"]'}</code></td>
    </tr>
    <tr>
      <td style={td0}>Tailwind utilities</td>
      <td style={td(false)}>Semantic utility classes mapped to CSS vars</td>
      <td style={td(false)}>Use in className strings: <code>bg-background</code>, <code>text-foreground</code>, <code>rounded-lg</code></td>
    </tr>
    <tr>
      <td style={td0}>JS/TS vars</td>
      <td style={td(false)}>CSS var reference strings for inline styles</td>
      <td style={td(false)}>CSS-in-JS, canvas, SSR snapshots</td>
    </tr>
  </tbody>
</table>

---

## Dark mode

Dark mode is controlled by the `.dark` class on any ancestor element (typically `<html>`).

**Automatic (ThemeProvider):**
```tsx
<ThemeProvider colorScheme="system">...</ThemeProvider>
```

**Manual toggle:**
```ts
document.documentElement.classList.toggle('dark', isDark)
```

---

## Tailwind preset

To use semantic utilities (`bg-background`, `text-foreground`, `rounded-md`, etc.) in your own project, point Tailwind at the package config:

```js
// tailwind.config.js
module.exports = {
  presets: [require('@geomak/ui/tailwind')],  // coming soon
  content: ['./src/**/*.{tsx,ts}'],
}
```

Until the preset is published, copy the `theme.extend` section from the package's `tailwind.config.cjs`.
